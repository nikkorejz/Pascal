/*
* Условие задачи:
* На стену (В = 3 метра, Д = 10 метров) нужно поклеить обои (Д = 10 метров), сколько нужно рулонов обоев всего?
* Результат должен быть целым числом
*/
Program oboi;
  Uses CRT;
  Var a,b:integer;
Begin
  clrscr;
  write('Введите высоту: ');
  readln(a);
  write('Введите длину: ');
  readln(b);
    if a * b mod 10 = 0 then 
  writeln (a * b div 10) else
  writeln((a * b div 10)+1);
  readkey;
End.
